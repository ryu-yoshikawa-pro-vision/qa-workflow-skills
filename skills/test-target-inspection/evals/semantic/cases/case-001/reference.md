# Reference
- 生きた実対象で今回範囲を確認し、画面・UI・挙動の現在値を取得する。
- version/buildが違う事実は両方の観測証跡として記録し、表示条件の比較可能性と分けて判断する。
- 削除候補はrole、viewport、locale、flag、data、到達経路の条件差で非表示になっていないことを確認する。削除後のcurrent本体テーブルに削除キーを残さない。
- 条件付き保存が使える場合は提供されたrevisionを使い、競合・保存後revisionを記録する。保存先の能力が不明なら自動上書きしない。
- 現在挙動は仕様Authorityではない。
