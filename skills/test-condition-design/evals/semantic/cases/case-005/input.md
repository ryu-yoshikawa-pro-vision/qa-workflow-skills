# Eval Input
検索APIの入力は整数`limit`で、有効範囲は0〜100 inclusive。通常値は25、50、75、境界値は0、100、異常値は-1、101とする。入力分布は通常値70%、境界値20%、異常値10%。categorical weightは順に通常値25=24、50=23、75=23、境界値0=10、100=10、異常値-1=5、101=5。固定seed=42、`case_count=10`のRandom Testingを設計する。サンプリングは復元抽出で、割合は抽選確率であり、生成後の実測件数を固定しない。
