# Eval Input
Source model `M-ORDERS` has current targets `A` and `B`. Model-wide requirement `R-ROLE` is `role ∈ {admin, guest}` and applies to both current targets. Target-specific requirements `R-A-LOW: version >= 2.0` and `R-A-HIGH: version <= 1.4` both reference current target A's exact source target version and therefore conflict only on A. Requirement `R-B-VERSION: version = 1.3` references current target B's exact source target version and applies only to B. All target-version references match the current target's source model, target_ref, target_content_fingerprint, and generation_fingerprint. Derive applicable target refs and report conflicts by affected target.
