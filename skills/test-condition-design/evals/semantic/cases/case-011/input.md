# Eval Input
画面にはreadonlyなtext control（pattern alias `input-text`）、disabledなnumber control（pattern alias `spinbutton`）、date control（unknown pattern `date-picker`）がある。UI pattern candidateを正規化する。disabled / readonly属性は入力制約へ変換しない。
