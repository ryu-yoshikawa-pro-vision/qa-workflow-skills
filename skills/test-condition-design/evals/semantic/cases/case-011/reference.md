# 判定根拠
現行runtime resultの`catalog_version`と`static_data_versions.ui_pattern_catalog`を保持する。今回のcatalog versionは`sha256:d03f2e9d12a84e28b33c058fd9798a7ecd2764da42f81d62c724a5abb2fb12c9`。`input-text`は`pattern_key=text-input`へ解決し、catalog候補`clear-paste`（clear / paste）、`length-boundary`（文字数境界）、`readonly-disabled`（readonly / disabled状態）、`required-empty`（required時の空入力）をruntime結果どおりに扱う。`spinbutton`は`pattern_key=number-input`へ解決し、`min-max-step`（min / max / step）と`non-numeric`（非数値入力）を扱う。これらは製品Authorityを必要とする一般候補であり、readonly / disabled属性からvalidation constraintや期待結果を作らない。catalogにない`date-picker`はruntimeの`unknown_pattern`としてunsupportedに残す。catalog候補の全件とkeyを保持することがcoverage candidate setであり、製品固有の期待結果を追加しない。HTML control typeのconstraint生成subsetは`schema_cases.py`の責務であり、UI pattern候補化できるtypeをここで制限しない。
